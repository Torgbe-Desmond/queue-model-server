function handleServerConnection(
    _serverData,
    serverSocketMap,
    registeredCompanies,
    availableServers,
    socket
){
    // Store socket ID if server is defined


}


module.exports = handleServerConnection 